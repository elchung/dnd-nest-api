TODO LIST
add source to all top level dtos and entities (phb, volos, etc)

finish entities -- verify item mapping is how i want

add backfill scripts for all new generation

add GET calls that can specify source (or anything really with object.add({}, whatever)

NO <ANY>

fill out mappers

add tests

add get/put/post/delete endpoint to items

FILL OUT LINTER WITH RULES I LIKE

once all data is in, turn off db syncronization and switch to migration scripts and make source column required.

rename entity and dto files to all lower case
